/*
import { TestBed } from '@angular/core/testing';

import { HeroAllResolver } from './hero-all-resolver.service';

describe('HeroAllResolver', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: HeroAllResolver = TestBed.get(HeroAllResolver);
    expect(service).toBeTruthy();
  });
});
*/